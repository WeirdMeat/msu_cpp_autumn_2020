#include "matrix.h"

int main(int argc, char const *argv[]) {
    matrix_wrong_init();
    matrix_cols();
    matrix_rows();
    matrix_el();
    matrix_output();
    matrix_comp();
    matrix_sum();
    matrix_mult();
    return 0;
}
